{
  "name": "validate-rules",
  "version": "0.2.0",
  "main": "validate-rules.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/niepsuj/validate-rules.git"
  },
  "scripts": {
    "build": "terser --compress --mangle --safari10 -- validate-rules.js > validate-rules.min.js"
  },
  "devDependencies": {
    "terser": "^4.6.3"
  }
}
